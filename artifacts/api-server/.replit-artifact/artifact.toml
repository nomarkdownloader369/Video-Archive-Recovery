kind = "web"
previewPath = "/api/"
title = "API Server"
description = "Express API server for PervFlix — serves video data, handles scraping from HQporner and GalaxyPorn, and manages the PostgreSQL video index."
version = "1.0.0"
id = "artifacts/api-server"
router = "path"

[[services]]
name = "web"
paths = [ "/api/" ]
localPort = 22729

[services.development]
run = "pnpm --filter @workspace/api-server run dev"

[services.production]

[services.production.build]
args = ["pnpm", "--filter", "@workspace/api-server", "run", "build"]

[services.production.build.env]
NODE_ENV = "production"

[services.production.run]
args = ["node", "--enable-source-maps", "artifacts/api-server/dist/index.mjs"]

[services.production.run.env]
NODE_ENV = "production"

[services.production.health.startup]
path = "/api/healthz"

[services.env]
PORT = "22729"
BASE_PATH = "/api/"
