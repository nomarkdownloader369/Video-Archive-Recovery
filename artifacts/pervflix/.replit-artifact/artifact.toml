kind = "web"
previewPath = "/"
title = "PervFlix"
description = "A cinematic adult video streaming platform that aggregates 25,000+ studio-quality videos from curated sources, served through a custom dark-themed React interface with search, categories, performer pages, and a personal watchlist."
version = "1.0.0"
id = "artifacts/pervflix"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22141

[services.development]
run = "pnpm --filter @workspace/pervflix run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/pervflix", "run", "build" ]
serve = "static"
publicDir = "artifacts/pervflix/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22141"
BASE_PATH = "/"
